---
phase: 01-web-fixes
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/App.tsx
  - index.html
autonomous: true
requirements:
  - UI-01
  - UI-02
  - AUDIO-01
  - TOUCH-01
  - VIEWPORT-01

must_haves:
  truths:
    - "During sequence playback, the active pad glows at full brightness with its colored glow shadow; inactive pads stay at full normal brightness — no dimming applied to any pad"
    - "Active pad does not receive the HTML disabled attribute during sequence playback"
    - "Audio plays on first game start in mobile Chrome without any user gesture workaround"
    - "Pad buttons register taps without 300ms delay on Android Chrome"
    - "Viewport does not zoom or shift when tapping pads on a mobile device"
  artifacts:
    - path: "src/App.tsx"
      provides: "Corrected pad class logic, awaited audio resume, touch-action inline styles"
    - path: "index.html"
      provides: "Updated viewport meta tag"
      contains: "user-scalable=no"
  key_links:
    - from: "src/App.tsx"
      to: "pad <button> elements"
      via: "interactiveClasses and disabled prop"
      pattern: "isDisabled && !isActive"
    - from: "src/App.tsx"
      to: "audioCtxRef.current.resume()"
      via: "handleStart and handlePadClick gesture handlers"
      pattern: "await audioCtxRef"
---

<objective>
Apply all five Phase 1 bug fixes to src/App.tsx and index.html: pad glow visual state (UI-01, UI-02), awaited AudioContext resume (AUDIO-01), touch-action manipulation on pads (TOUCH-01), and viewport meta tag (VIEWPORT-01).

Purpose: These are the observable fixes that make the game work correctly on Android Chrome — correct pad glow during sequence playback, reliable audio on first gesture, no 300ms tap delay, and no accidental viewport zoom.

Output: A corrected App.tsx and index.html with all five requirements satisfied and npm run build still passing.
</objective>

<execution_context>
@C:/Users/omerb/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/omerb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-web-fixes/01-CONTEXT.md
@.planning/phases/01-web-fixes/01-UI-SPEC.md
@.planning/phases/01-web-fixes/01-01-SUMMARY.md
</context>

<interfaces>
<!-- Key contracts extracted from App.tsx that the executor needs. -->

From src/App.tsx (lines 321-349) — current buggy pad rendering loop:
  const isActive = activePad === pad.id;   // line 322 — already computed, use this
  interactiveClasses = isDisabled ? 'cursor-not-allowed opacity-40' : 'cursor-pointer hover:brightness-110'
  disabled={isDisabled}

Target state per 01-UI-SPEC.md §Pad Visual State Contract:
  interactiveClasses = isDisabled && !isActive
    ? 'cursor-not-allowed'
    : !isDisabled
      ? 'cursor-pointer hover:brightness-110'
      : ''
  disabled={isDisabled && !isActive}
  style={{ touchAction: 'manipulation' }}   // added to same <button> element

From src/App.tsx (line 87-90) — current buggy audio resume:
  if (audioCtxRef.current.state === 'suspended') {
    void audioCtxRef.current.resume();   // not awaited — REMOVE this entire if-block
  }

Target state per D-04:
  In handleStart and handlePadClick (both already async):
    try { await audioCtxRef.current.resume(); } catch (_) {}
  Remove the void resume() from ensureAudioContext entirely.
  Do NOT make ensureAudioContext async.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Fix pad glow, disabled attribute, and touch-action in App.tsx (UI-01, UI-02, TOUCH-01)</name>
  <files>src/App.tsx</files>
  <read_first>
    - src/App.tsx — read in full; the pad rendering loop is at lines 321-349; isActive is already computed at line 322 — no new state needed; interactiveClasses and disabled prop are the two targets; the <button> element starts at line 333
    - .planning/phases/01-web-fixes/01-UI-SPEC.md §Pad Visual State Contract — authoritative class assembly rule and the three-state table (Active / Interactive / Idle)
    - .planning/phases/01-web-fixes/01-CONTEXT.md §Decisions D-01, D-02, D-03 — no opacity-40 on any pad in any state, ever
  </read_first>
  <action>
    Make three surgical changes inside the PADS.map() rendering loop in App.tsx:

    Change 1 — interactiveClasses (replaces lines 325-327):
    Replace the current two-branch ternary with a three-branch expression that uses the already-computed isActive boolean:
    - When isDisabled is true AND isActive is false: apply 'cursor-not-allowed' only (no opacity-40)
    - When isDisabled is false: apply 'cursor-pointer hover:brightness-110'
    - When isDisabled is true AND isActive is true: apply '' (empty string — pad is being shown active, no cursor class)
    The string 'opacity-40' must not appear anywhere in this file after the edit.

    Change 2 — disabled prop (line 336):
    Change disabled={isDisabled} to disabled={isDisabled && !isActive}.

    Change 3 — touchAction inline style (on the same <button> element):
    Add style={{ touchAction: 'manipulation' }} as a JSX prop to the pad <button> element. Place it after the disabled prop and before onClick. Do not add this style to any other button (Start Game and Reset buttons are not timing-sensitive).

    Do not change baseClasses, activeClasses, the class join array, or any other part of the component.
  </action>
  <verify>
    <automated>cd C:\code\cursor\SimonGame && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - The string 'opacity-40' does not appear anywhere in src/App.tsx
    - interactiveClasses assignment contains both 'isDisabled && !isActive' and '!isDisabled' branches
    - The pad <button> has disabled={isDisabled && !isActive} (not just disabled={isDisabled})
    - The pad <button> has style={{ touchAction: 'manipulation' }} as a JSX prop
    - The Start Game button and Reset button do NOT have touchAction style
    - npm run build exits 0
    - During sequence playback in the browser: the active pad shows its glowClass shadow and brightness-125 without any dimming; inactive pads show at full normal brightness
    - During sequence playback in the browser: the active pad does not have a disabled attribute in the DOM (verify via DevTools Elements panel)
  </acceptance_criteria>
  <done>App.tsx pad loop uses the three-branch interactiveClasses, disabled={isDisabled && !isActive}, and style={{ touchAction: 'manipulation' }}; build passes; opacity-40 is absent from the file</done>
</task>

<task type="auto">
  <name>Task 2: Await AudioContext resume in gesture handlers (AUDIO-01)</name>
  <files>src/App.tsx</files>
  <read_first>
    - src/App.tsx — focus on ensureAudioContext (lines 78-91), handleStart (lines 197-207), and handlePadClick (lines 209-283); both handleStart and handlePadClick are already async functions — no signature changes needed
    - .planning/phases/01-web-fixes/01-CONTEXT.md §Decisions D-04, D-05 — await resume() in gesture handlers, silent try/catch, do NOT make ensureAudioContext async
  </read_first>
  <action>
    Make two changes to fix the audio resume:

    Change 1 — remove void resume() from ensureAudioContext (lines 87-89):
    Delete the entire if-block that calls void audioCtxRef.current.resume(). After deletion, ensureAudioContext only creates the AudioContext+GainNode on first call and returns audioCtxRef.current. The function signature and return type stay unchanged (synchronous).

    Change 2 — add awaited resume() to handleStart and handlePadClick:
    In handleStart, immediately before the await playSequence(...) call, insert:
      try { await audioCtxRef.current?.resume(); } catch (_) {}
    In handlePadClick, immediately after the guard clause (the early return at line 211), insert the same:
      try { await audioCtxRef.current?.resume(); } catch (_) {}

    The optional-chaining (?.) guards against the case where audioCtxRef.current is null before the first gesture creates it. The try/catch swallows any NotAllowedError silently per D-04.

    Do not touch playSequence, playTone, playColorTone, or any other function.
  </action>
  <verify>
    <automated>cd C:\code\cursor\SimonGame && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - ensureAudioContext does not contain 'void audioCtxRef.current.resume()' or any resume() call
    - handleStart contains 'await audioCtxRef.current?.resume()' wrapped in try/catch
    - handlePadClick contains 'await audioCtxRef.current?.resume()' wrapped in try/catch
    - ensureAudioContext is not declared as async (no 'async' keyword on its declaration)
    - npm run build exits 0
    - In a mobile Chrome browser (or DevTools device emulation): tapping Start Game on the first gesture produces audible tones without any additional workaround
  </acceptance_criteria>
  <done>ensureAudioContext no longer calls resume(); handleStart and handlePadClick each await resume() with try/catch; build passes</done>
</task>

<task type="auto">
  <name>Task 3: Update viewport meta tag in index.html (VIEWPORT-01)</name>
  <files>index.html</files>
  <read_first>
    - index.html — the existing viewport tag is on line 6: &lt;meta name="viewport" content="width=device-width, initial-scale=1.0" /&gt; — modify this tag in place, do not add a second one
    - .planning/phases/01-web-fixes/01-UI-SPEC.md §Viewport Meta Tag — exact target content string
  </read_first>
  <action>
    Replace the content attribute of the existing &lt;meta name="viewport"&gt; tag with:
      width=device-width, initial-scale=1.0, user-scalable=no, viewport-fit=cover

    Edit the tag in place (line 6). Do not add a second meta viewport tag. No other changes to index.html.
  </action>
  <verify>
    <automated>cd C:\code\cursor\SimonGame && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - index.html contains exactly one &lt;meta name="viewport"&gt; tag
    - That tag's content attribute is exactly: width=device-width, initial-scale=1.0, user-scalable=no, viewport-fit=cover
    - index.html does not contain a second viewport meta tag
    - npm run build exits 0
  </acceptance_criteria>
  <done>index.html viewport tag includes user-scalable=no and viewport-fit=cover; build passes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user gesture → AudioContext.resume() | Browser audio API called in response to touch/click events |
| pad button → React event handler | onClick fires handlePadClick with user-supplied color ID (from PADS constant, not external input) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Denial of Service | AudioContext.resume() | mitigate | try/catch wraps await resume() per D-04 — audio failure is silent, game continues |
| T-02-02 | Tampering | pad onClick handler | accept | color argument sourced from PADS constant, not user text input — no injection surface |
| T-02-03 | Information Disclosure | viewport-fit=cover | accept | CSS viewport hint; no PII exposure |
| T-02-SC | Tampering | npm installs | accept | Plan 02 installs no new packages — no package legitimacy gate required |
</threat_model>

<verification>
After both tasks complete, run `npm run build` one final time to confirm zero errors across all edits. Then open the app in Chrome DevTools with mobile device emulation and verify:

1. Click Start Game — audible tones play immediately (no second tap required)
2. During sequence playback — active pad glows with colored shadow at full brightness; other pads remain at full normal brightness (not dimmed)
3. DOM inspection during sequence — active pad element has no `disabled` attribute; inactive pads have `disabled` attribute
4. Pad buttons have `style="touch-action: manipulation"` in DOM
5. Viewport meta tag in page source: `width=device-width, initial-scale=1.0, user-scalable=no, viewport-fit=cover`
</verification>

<success_criteria>
- npm run build exits 0
- 'opacity-40' does not appear in src/App.tsx
- interactiveClasses uses isDisabled && !isActive conditional
- disabled={isDisabled && !isActive} on pad buttons
- style={{ touchAction: 'manipulation' }} on pad buttons only
- ensureAudioContext has no resume() call
- handleStart and handlePadClick both contain awaited resume() with try/catch
- index.html viewport meta includes user-scalable=no and viewport-fit=cover
</success_criteria>

<output>
Create `.planning/phases/01-web-fixes/01-02-SUMMARY.md` when done.
</output>
