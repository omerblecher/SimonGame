---
phase: 01-web-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tsconfig.json
  - src/main.ts
  - src/counter.ts
  - src/typescript.svg
autonomous: true
requirements:
  - UI-01
  - UI-02
  - AUDIO-01
  - TOUCH-01
  - VIEWPORT-01

must_haves:
  truths:
    - "npm run build exits 0 with zero TypeScript errors"
    - "No unused Vite scaffold files remain in src/"
  artifacts:
    - path: "tsconfig.json"
      provides: "JSX transform configuration for React 19"
      contains: '"jsx": "react-jsx"'
  key_links:
    - from: "tsconfig.json"
      to: "src/App.tsx"
      via: "TypeScript JSX compilation"
      pattern: '"jsx".*"react-jsx"'
---

<objective>
Unblock the build: add the missing JSX compiler option to tsconfig.json and remove three dead Vite scaffold files that clutter the TypeScript include glob.

Purpose: `npm run build` currently fails with ~150 TS17004 errors because `"jsx": "react-jsx"` is absent from compilerOptions. No subsequent plan can be verified until this is fixed. The dead files (`src/main.ts`, `src/counter.ts`, `src/typescript.svg`) are Vite template stubs never referenced by the actual app — `src/main.ts` imports `counter.ts`, so both must be deleted together to avoid introducing a new TS error.

Output: A passing `npm run build` on a clean codebase.
</objective>

<execution_context>
@C:/Users/omerb/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/omerb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add JSX compiler option and delete dead scaffold files</name>
  <files>tsconfig.json, src/main.ts, src/counter.ts, src/typescript.svg</files>
  <read_first>
    - tsconfig.json — read current compilerOptions before editing; the "jsx" key is absent and must be added inside "compilerOptions"
    - src/main.ts — confirm it imports counter.ts (it does — both must be deleted atomically)
    - src/counter.ts — confirm it is the only export consumer of src/main.ts (it is — safe to delete)
  </read_first>
  <action>
    In tsconfig.json, add "jsx": "react-jsx" inside compilerOptions. Place it immediately after the "types" line (line 7 in current file). Do not change any other key.

    Delete the three dead Vite scaffold files: src/main.ts, src/counter.ts, src/typescript.svg. These are original Vite template stubs — none are imported by src/main.tsx or src/App.tsx. Deleting main.ts without counter.ts (or vice versa) would leave a broken import; delete both together. typescript.svg is referenced only by the deleted main.ts so it can go too.

    After the edits, run npm run build to confirm zero errors.
  </action>
  <verify>
    <automated>cd C:\code\cursor\SimonGame && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - tsconfig.json compilerOptions contains the key-value pair "jsx": "react-jsx"
    - src/main.ts does not exist
    - src/counter.ts does not exist
    - npm run build exits with code 0 and prints no TypeScript diagnostic errors
    - dist/ directory is created or updated with output files
  </acceptance_criteria>
  <done>npm run build passes with zero errors; dead scaffold files are gone from src/</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer machine → build output | Local TypeScript compilation; no external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | tsconfig.json | accept | Config is developer-controlled; no user input crosses this boundary |
| T-01-SC | Tampering | npm installs | accept | Plan 01 installs no new packages — no package legitimacy gate required |
</threat_model>

<verification>
Run `npm run build` from the project root. Expected: exits 0, no diagnostic output, `dist/` directory populated.
</verification>

<success_criteria>
- `npm run build` exits 0
- `tsconfig.json` contains `"jsx": "react-jsx"` in compilerOptions
- `src/main.ts`, `src/counter.ts`, `src/typescript.svg` are deleted
</success_criteria>

<output>
Create `.planning/phases/01-web-fixes/01-01-SUMMARY.md` when done.
</output>
