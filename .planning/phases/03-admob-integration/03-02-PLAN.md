---
phase: 03-admob-integration
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/hooks/useBannerHeight.ts
  - src/main.tsx
autonomous: true
requirements:
  - ADM-03
  - ADM-04
  - ADM-07
  - ADM-08

must_haves:
  truths:
    - "src/hooks/ directory exists and contains useBannerHeight.ts"
    - "useBannerHeight() returns 0 when !Capacitor.isNativePlatform()"
    - "useBannerHeight() returns 50 as initial value on native, then updates to actual banner height via bannerAdSizeChanged"
    - "main.tsx runs UMP consent THEN AdMob.initialize() THEN ReactDOM.render() — in that exact order"
    - "If consent or initialize throws, the game renders anyway (graceful degradation)"
    - "All AdMob calls in main.tsx are behind Capacitor.isNativePlatform() guard"
    - "npm run build exits 0 after all changes"
  artifacts:
    - path: "src/hooks/useBannerHeight.ts"
      provides: "bannerAdSizeChanged listener hook"
      exports: ["useBannerHeight"]
    - path: "src/main.tsx"
      provides: "pre-render AdMob init sequence"
      contains: "initializeAdMob"
  key_links:
    - from: "src/main.tsx IIFE"
      to: "AdMob.requestConsentInfo() then AdMob.showConsentForm() then AdMob.initialize()"
      via: "async IIFE with try/catch"
      pattern: "async.*initializeAdMob"
    - from: "useBannerHeight.ts"
      to: "BannerAdPluginEvents.SizeChanged"
      via: "AdMob.addListener"
      pattern: "BannerAdPluginEvents.SizeChanged"
---

<objective>
Create the `src/hooks/useBannerHeight.ts` hook and rewrite `src/main.tsx` to implement the pre-render GDPR/UMP consent + AdMob initialization sequence.

Purpose: These two files are the core of the AdMob integration. useBannerHeight.ts encapsulates the banner size listener (isolated from game logic, per D-04). main.tsx implements the mandatory consent→init→render ordering (ADM-03, ADM-04) using an IIFE pattern that gracefully degrades if the SDK fails.

Output: New hook file at src/hooks/useBannerHeight.ts; rewritten src/main.tsx (~30 lines). Both files compile cleanly with TypeScript strict mode.
</objective>

<execution_context>
@C:/Users/omerb/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/omerb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/code/cursor/SimonGame/.planning/PROJECT.md
@C:/code/cursor/SimonGame/.planning/ROADMAP.md
@C:/code/cursor/SimonGame/.planning/STATE.md
@C:/code/cursor/SimonGame/.planning/phases/03-admob-integration/03-CONTEXT.md
@C:/code/cursor/SimonGame/.planning/phases/03-admob-integration/03-RESEARCH.md
@C:/code/cursor/SimonGame/.planning/phases/03-admob-integration/03-UI-SPEC.md

<interfaces>
<!-- Current main.tsx (8 lines — full file) -->
import React from 'react';
import ReactDOM from 'react-dom/client';
import './style.css';
import { App } from './App';

ReactDOM.createRoot(document.getElementById('app') as HTMLElement).render(
  &lt;React.StrictMode&gt;
    &lt;App /&gt;
  &lt;/React.StrictMode&gt;,
);

<!-- tsconfig.json critical settings (verified) -->
"target": "ES2022",
"module": "ESNext"
--> Top-level await IS supported. IIFE pattern is used anyway (safer with strict mode — no TS1378 risk).

<!-- API types from @capacitor-community/admob@8.0.0 (verified from npm tarball dist/esm/*.d.ts) -->

// Consent API (dist/esm/consent/consent-definition.interface.d.ts):
interface AdmobConsentInfo {
  status: AdmobConsentStatus;
  isConsentFormAvailable?: boolean;
  canRequestAds: boolean;
  privacyOptionsRequirementStatus: PrivacyOptionsRequirementStatus;
}
AdMob.requestConsentInfo(): Promise&lt;AdmobConsentInfo&gt;
AdMob.showConsentForm(): Promise&lt;AdmobConsentInfo&gt;

// Initialization API (dist/esm/definitions.d.ts):
interface AdMobInitializationOptions {
  testingDevices?: string[];
  initializeForTesting?: boolean;   // set true for Phase 3
  tagForChildDirectedTreatment?: boolean;
  tagForUnderAgeOfConsent?: boolean;
  maxAdContentRating?: MaxAdContentRating;
}
AdMob.initialize(options?: AdMobInitializationOptions): Promise&lt;void&gt;

// Banner size event (dist/esm/banner/banner-definitions.interface.d.ts):
// CRITICAL: use bannerAdSizeChanged (NOT bannerAdLoaded) for height payload
BannerAdPluginEvents.SizeChanged = "bannerAdSizeChanged"
// Payload: AdMobBannerSize { width: number; height: number }
// bannerAdLoaded listener is () => void — NO height payload

// Listener handle type:
AdMob.addListener(event, handler): Promise&lt;{ remove: () => Promise&lt;void&gt; }&gt;

// Capacitor guard:
import { Capacitor } from '@capacitor/core';
Capacitor.isNativePlatform(): boolean   // false in browser, true on Android
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create src/hooks/useBannerHeight.ts</name>
  <files>src/hooks/useBannerHeight.ts</files>
  <read_first>
    - C:/code/cursor/SimonGame/.planning/phases/03-admob-integration/03-RESEARCH.md Pattern 2 — complete hook implementation with BannerAdPluginEvents.SizeChanged (not bannerAdLoaded)
    - C:/code/cursor/SimonGame/.planning/phases/03-admob-integration/03-UI-SPEC.md §Spacing Scale — spacer height values: 0 in browser, 50 pre-load default on native, actual height post-bannerAdSizeChanged
    - C:/code/cursor/SimonGame/tsconfig.json — confirm strict mode is on (noUnusedLocals, noUnusedParameters — hook must not have unused variables)
  </read_first>
  <action>
    Create the directory src/hooks/ and the file src/hooks/useBannerHeight.ts.

    The hook must:
    1. Import: useEffect, useState from 'react'; Capacitor from '@capacitor/core'; AdMob and BannerAdPluginEvents from '@capacitor-community/admob'
    2. Declare BANNER_HEIGHT_DEFAULT = 50 as a module-level const (standard banner dp)
    3. Export function useBannerHeight(): number
    4. useState initial value: Capacitor.isNativePlatform() ? BANNER_HEIGHT_DEFAULT : 0
    5. useEffect: guard with if (!Capacitor.isNativePlatform()) return — this satisfies D-06
    6. Inside useEffect: call AdMob.addListener(BannerAdPluginEvents.SizeChanged, (size) => { setBannerHeight(size.height); }) and store the returned Promise&lt;handle&gt; in a nullable let variable
    7. useEffect cleanup: call handle?.remove() to deregister the listener on unmount
    8. Return bannerHeight

    Use BannerAdPluginEvents.SizeChanged (the enum value, which resolves to the string "bannerAdSizeChanged") — NOT the string literal "bannerAdLoaded". This is the critical deviation from CONTEXT.md D-02/ADM-07 wording; RESEARCH.md confirms bannerAdLoaded has no payload. The RESEARCH.md open question 2 resolution is: use SizeChanged.

    The handle variable must be typed as PluginListenerHandle | null where PluginListenerHandle is the type returned by addListener (it has a .remove() method). Import the type if needed or use the inferred type from the Promise. With strict noUnusedLocals, ensure handle is used in the cleanup.

    TypeScript strict mode is on (strict: true, noUnusedLocals: true, noUnusedParameters: true). The hook must have zero unused variables.

    Follow Pattern 2 from RESEARCH.md exactly. Do not deviate from the addListener→then(h => { handle = h }) + cleanup pattern shown there.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - File src/hooks/useBannerHeight.ts exists
    - File exports function useBannerHeight(): number (exact signature)
    - File imports BannerAdPluginEvents from '@capacitor-community/admob'
    - File uses BannerAdPluginEvents.SizeChanged (NOT the string "bannerAdLoaded" or BannerAdPluginEvents.Loaded)
    - File contains Capacitor.isNativePlatform() guard in useEffect
    - Initial useState value is conditional: BANNER_HEIGHT_DEFAULT (50) on native, 0 in browser
    - useEffect cleanup calls handle?.remove()
    - npm run build exits 0 with zero TypeScript errors (strict mode must pass)
  </acceptance_criteria>
  <done>useBannerHeight hook created; bannerAdSizeChanged listener; returns 0 in browser, 50 pre-load on native</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite src/main.tsx with IIFE consent+init sequence</name>
  <files>src/main.tsx</files>
  <read_first>
    - C:/code/cursor/SimonGame/src/main.tsx — read the current 8-line file; it will be fully replaced
    - C:/code/cursor/SimonGame/.planning/phases/03-admob-integration/03-RESEARCH.md Pattern 1 — UMP consent + AdMob.initialize() sequence; note the IIFE variant in Pitfall 3 (safer with strict TS)
    - C:/code/cursor/SimonGame/.planning/phases/03-admob-integration/03-CONTEXT.md §D-09 and D-10 — consent runs before render; always run UMP regardless of region
    - C:/code/cursor/SimonGame/tsconfig.json — "module": "ESNext" and "target": "ES2022" confirms top-level await is valid, but IIFE is used per planning decision for strictness safety
  </read_first>
  <action>
    Replace the entire content of src/main.tsx with the IIFE-based initialization sequence. The new file structure:

    Imports (in order):
    1. import React from 'react'
    2. import ReactDOM from 'react-dom/client'
    3. import './style.css'
    4. import { App } from './App'
    5. import { Capacitor } from '@capacitor/core'
    6. import { AdMob } from '@capacitor-community/admob'

    Helper function initializeAdMob (async, returns Promise&lt;void&gt;):
    - Calls AdMob.requestConsentInfo() and stores result as consentInfo
    - Checks: if (!consentInfo.canRequestAds && consentInfo.isConsentFormAvailable === true) then await AdMob.showConsentForm()
    - Calls await AdMob.initialize({ initializeForTesting: true })
    - No return value needed

    The root setup and IIFE (must follow the function definition):
    - const rootEl = document.getElementById('app') as HTMLElement
    - const root = ReactDOM.createRoot(rootEl)
    - Then the IIFE: (async () => { ... })()

    Inside the IIFE:
    - if (Capacitor.isNativePlatform()) { try { await initializeAdMob(); } catch (err) { console.warn('AdMob initialization failed, rendering without ads:', err); } }
    - root.render(&lt;React.StrictMode&gt;&lt;App /&gt;&lt;/React.StrictMode&gt;)

    The render() call is UNCONDITIONAL — it always runs whether or not AdMob init succeeded (graceful degradation per D-09 and Claude's Discretion note in CONTEXT.md).

    The App import stays as { App } from './App' — same as before (no alias needed here; App collision only matters in App.tsx where CapacitorApp alias is used).

    After writing the file, run npm run build to verify TypeScript compilation. The App.tsx import of App is not affected.

    Key constraints:
    - initializeForTesting: true in AdMob.initialize() options — Phase 3 only (per planning key decision #9)
    - The isConsentFormAvailable check uses === true (not just truthiness) to be explicit with TypeScript's optional boolean type
    - Do not call showConsentForm() unconditionally — check canRequestAds first (per RESEARCH.md §State of the Art and Consent API pattern)
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - src/main.tsx contains async function initializeAdMob (or equivalent inline async function)
    - src/main.tsx contains AdMob.requestConsentInfo() call
    - src/main.tsx contains AdMob.initialize({ initializeForTesting: true }) call
    - src/main.tsx contains the IIFE pattern: (async () => { ... })()
    - src/main.tsx contains root.render() call OUTSIDE any try/catch (always executes)
    - src/main.tsx contains Capacitor.isNativePlatform() guard wrapping the AdMob calls
    - The ordering in the IIFE is: initializeAdMob() → root.render() (never render before init completes on native)
    - npm run build exits 0 with zero TypeScript errors
    - dist/index.html is present after build (Vite built successfully)
  </acceptance_criteria>
  <done>main.tsx implements consent→init→render; graceful degradation on failure; no-op in browser</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UMP SDK response → app code | consentInfo returned from Google SDK determines whether consent form is shown |
| AdMob.initialize() → React render | JS awaits native SDK init; a hung promise would block the game rendering |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Denial of Service | AdMob.initialize() hanging indefinitely | mitigate | IIFE try/catch ensures root.render() always executes even if initializeAdMob() throws or rejects; game is never blocked |
| T-03-05 | Information Disclosure | initializeForTesting: true flag in production | accept | This flag is Phase 3 only; RESEARCH.md planning key decision #9 notes it is replaced with false in Phase 4. Low risk: test mode serves test ads, not real revenue — no user data exposure |
| T-03-06 | Tampering | Hook cleanup — listener not removed on unmount | mitigate | useEffect cleanup calls handle?.remove(); prevents stale listener accumulating across React StrictMode double-invocations |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | Packages already installed in Plan 01; no new installs in this plan |
</threat_model>

<verification>
After both tasks complete:

1. src/hooks/useBannerHeight.ts exists — ls src/hooks/
2. npm run build exits 0 — zero TypeScript errors in both new files
3. src/main.tsx contains "initializeAdMob" and "requestConsentInfo" and "isNativePlatform"
4. src/hooks/useBannerHeight.ts contains "BannerAdPluginEvents.SizeChanged" (not "bannerAdLoaded")
5. src/main.tsx contains root.render() after the IIFE conditional block
</verification>

<success_criteria>
- ADM-03: GDPR/UMP consent flow runs before AdMob.initialize() — SATISFIED by the requestConsentInfo → showConsentForm → initialize sequence in initializeAdMob()
- ADM-04: AdMob.initialize() awaited before React renders — SATISFIED by await initializeAdMob() in IIFE before root.render()
- ADM-07: bannerAdSizeChanged used for dynamic spacer height — SATISFIED by BannerAdPluginEvents.SizeChanged in useBannerHeight.ts
- ADM-08 (partial): @capacitor/app installed (Plan 01) and Capacitor type available — fully wired in Plan 03
- npm run build exits 0 after all changes
</success_criteria>

<output>
Create `.planning/phases/03-admob-integration/03-02-SUMMARY.md` when done
</output>
