---
phase: 03-admob-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - android/app/src/main/AndroidManifest.xml
  - android/app/src/main/res/values/strings.xml
  - index.html
autonomous: true
requirements:
  - ADM-01
  - ADM-02

must_haves:
  truths:
    - "@capacitor-community/admob@8.0.0 and @capacitor/app are in package.json dependencies"
    - "AndroidManifest.xml contains the AdMob APPLICATION_ID meta-data element pointing to @string/admob_app_id"
    - "strings.xml contains a string resource named admob_app_id with the YOUR_ADMOB_APP_ID placeholder"
    - "index.html body tag has background-color #0f172a inline style"
    - "npx cap sync android runs cleanly after package installation"
  artifacts:
    - path: "package.json"
      provides: "admob and capacitor/app dependencies"
      contains: "@capacitor-community/admob"
    - path: "android/app/src/main/AndroidManifest.xml"
      provides: "AdMob App ID meta-data declaration"
      contains: "com.google.android.gms.ads.APPLICATION_ID"
    - path: "android/app/src/main/res/values/strings.xml"
      provides: "AdMob App ID string resource"
      contains: "admob_app_id"
    - path: "index.html"
      provides: "pre-render loading state background"
      contains: "background-color: #0f172a"
  key_links:
    - from: "AndroidManifest.xml meta-data"
      to: "strings.xml admob_app_id"
      via: "@string/admob_app_id reference"
      pattern: "android:value=\"@string/admob_app_id\""

user_setup:
  - service: admob
    why: "App ID is required in strings.xml before running the Android build in Plan 03"
    steps:
      - task: "Register Simon game in AdMob console"
        location: "https://admob.google.com → Apps → Add App → Android → app name: Simon Memory Game → package: com.otis.brooke.simon.game"
        result: "Receive App ID in format ca-app-pub-XXXXXXXXXXXXXXXX~XXXXXXXXXX (note: tilde separator)"
      - task: "Replace placeholder in strings.xml"
        location: "android/app/src/main/res/values/strings.xml line: <string name=\"admob_app_id\">YOUR_ADMOB_APP_ID</string>"
        result: "Replace YOUR_ADMOB_APP_ID with the real App ID before Plan 03 build"
---

<objective>
Install the AdMob and Capacitor App npm packages, add the required Android native configuration for the AdMob SDK (App ID meta-data + string resource), and set the pre-render body background color in index.html.

Purpose: Establishes the two hard prerequisites for the JS integration in Plan 02 — packages available for import, and Android native SDK configuration present so `npx cap sync android` wires the plugin into the Gradle build.

Output: Updated package.json, AndroidManifest.xml (with meta-data), strings.xml (with admob_app_id placeholder), index.html (with body background). Running `npx cap sync android` after this plan completes will pull the Google Play Services Ads and UMP SDK into the Gradle dependency graph.
</objective>

<execution_context>
@C:/Users/omerb/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/omerb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/code/cursor/SimonGame/.planning/PROJECT.md
@C:/code/cursor/SimonGame/.planning/ROADMAP.md
@C:/code/cursor/SimonGame/.planning/STATE.md
@C:/code/cursor/SimonGame/.planning/phases/03-admob-integration/03-CONTEXT.md
@C:/code/cursor/SimonGame/.planning/phases/03-admob-integration/03-RESEARCH.md
@C:/code/cursor/SimonGame/.planning/phases/03-admob-integration/03-UI-SPEC.md

<interfaces>
<!-- Current AndroidManifest.xml structure (Phase 2 final state) -->
<!-- The <application> block ends at line 37 with </application> -->
<!-- The AdMob meta-data must be added INSIDE the <application> block, before </application> -->
<!-- portrait lock already present: android:screenOrientation="portrait" on MainActivity activity element -->

From android/app/src/main/AndroidManifest.xml:
  - <application> block: lines 4-37
  - INTERNET permission: already declared on line 41
  - No existing meta-data for AdMob

From android/app/src/main/res/values/strings.xml:
  - <string name="app_name">Simon Memory Game</string>
  - <string name="title_activity_main">Simon Memory Game</string>
  - <string name="package_name">com.otis.brooke.simon.game</string>
  - <string name="custom_url_scheme">com.otis.brooke.simon.game</string>
  - admob_app_id string: NOT present yet — must be added

From index.html:
  - <body> tag on line 9: currently <body> with no style attribute
  - Must become: <body style="background-color: #0f172a;">

Package legitimacy audit (from RESEARCH.md):
  - @capacitor-community/admob@8.0.0 — [OK] Approved
  - @capacitor/app — [OK] Approved
  - No postinstall scripts on either package
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install admob and @capacitor/app packages</name>
  <files>package.json</files>
  <read_first>
    - C:/code/cursor/SimonGame/package.json — verify current @capacitor/core version (must be ^8.x) and confirm neither @capacitor-community/admob nor @capacitor/app are already present
  </read_first>
  <action>
    Run both installs sequentially. Use exact version for admob to match the Capacitor 8 peer dep:

    1. Run: npm install @capacitor-community/admob@8.0.0
    2. Run: npm install @capacitor/app

    Both packages are approved in the RESEARCH.md Package Legitimacy Audit ([OK] for both). No human checkpoint is needed before install.

    After install, run: npm run build
    This verifies the new packages do not break TypeScript compilation and the dist/ is valid for cap sync.

    Then run: $env:JAVA_HOME = "C:/Program Files/Eclipse Adoptium/jdk-21.0.7.6-hotspot"; npx cap sync android
    This wires the admob plugin's Gradle dependencies (Google Play Services Ads 24.9.+, UMP SDK 4.0.0) into the Android project. No build.gradle edits are needed — the plugin's own build.gradle declares these.
  </action>
  <verify>
    <automated>npm list @capacitor-community/admob @capacitor/app</automated>
  </verify>
  <acceptance_criteria>
    - npm list output shows: @capacitor-community/admob@8.0.0 and @capacitor/app@8.x.x (or latest ^8)
    - npm run build exits 0 (tsc + Vite — no compilation errors from new packages)
    - npx cap sync android exits 0 — no Gradle sync errors
    - package.json contains both "@capacitor-community/admob" and "@capacitor/app" in dependencies
  </acceptance_criteria>
  <done>Both packages installed, build passes, cap sync succeeds</done>
</task>

<task type="auto">
  <name>Task 2: Add AdMob App ID to AndroidManifest.xml and strings.xml</name>
  <files>android/app/src/main/AndroidManifest.xml, android/app/src/main/res/values/strings.xml</files>
  <read_first>
    - C:/code/cursor/SimonGame/android/app/src/main/AndroidManifest.xml — read full file; the meta-data element goes inside the &lt;application&gt; block, after the &lt;/provider&gt; closing tag and before &lt;/application&gt;
    - C:/code/cursor/SimonGame/android/app/src/main/res/values/strings.xml — read full file; the new string resource is appended inside &lt;resources&gt;
    - C:/code/cursor/SimonGame/.planning/phases/03-admob-integration/03-RESEARCH.md Pattern 4 — exact XML to add (AndroidManifest meta-data format and strings.xml format)
  </read_first>
  <action>
    AndroidManifest.xml change (ADM-02):
    Add the following meta-data element inside the &lt;application&gt; block, positioned after the &lt;/provider&gt; closing tag and before &lt;/application&gt;:

      &lt;meta-data
          android:name="com.google.android.gms.ads.APPLICATION_ID"
          android:value="@string/admob_app_id"/&gt;

    The android:value MUST be "@string/admob_app_id" — NOT a hardcoded ID value directly in the attribute (per D-07 and Pattern 4 from RESEARCH.md). This references the strings.xml resource.

    No permission additions needed: the INTERNET permission is already present on line 41, and the plugin's own merged AndroidManifest.xml handles ACCESS_NETWORK_STATE.

    strings.xml change:
    Add the following string resource inside the &lt;resources&gt; element, after the last existing &lt;string&gt; entry:

      &lt;string name="admob_app_id"&gt;YOUR_ADMOB_APP_ID&lt;/string&gt;

    The placeholder value YOUR_ADMOB_APP_ID stays as-is. The user will replace it with their real AdMob App ID (format: ca-app-pub-XXXXXXXXXXXXXXXX~XXXXXXXXXX, tilde separator) before running the build in Plan 03. This is documented in user_setup above.

    Do NOT add a &lt;uses-permission&gt; for ACCESS_NETWORK_STATE — the plugin's merged manifest handles it.
  </action>
  <verify>
    <automated>
      grep -c "com.google.android.gms.ads.APPLICATION_ID" "C:/code/cursor/SimonGame/android/app/src/main/AndroidManifest.xml"
      grep -c "admob_app_id" "C:/code/cursor/SimonGame/android/app/src/main/res/values/strings.xml"
    </automated>
  </verify>
  <acceptance_criteria>
    - AndroidManifest.xml contains exactly one &lt;meta-data android:name="com.google.android.gms.ads.APPLICATION_ID" android:value="@string/admob_app_id"/&gt; element inside the &lt;application&gt; block
    - AndroidManifest.xml is valid XML (no malformed tags; existing portrait lock, provider, and permissions unchanged)
    - strings.xml contains &lt;string name="admob_app_id"&gt;YOUR_ADMOB_APP_ID&lt;/string&gt; inside &lt;resources&gt;
    - strings.xml existing entries (app_name, title_activity_main, package_name, custom_url_scheme) are unchanged
  </acceptance_criteria>
  <done>AndroidManifest.xml has AdMob meta-data; strings.xml has admob_app_id placeholder</done>
</task>

<task type="auto">
  <name>Task 3: Add pre-render body background to index.html</name>
  <files>index.html</files>
  <read_first>
    - C:/code/cursor/SimonGame/index.html — read the file; &lt;body&gt; tag is currently on line 9 with no style attribute
    - C:/code/cursor/SimonGame/.planning/phases/03-admob-integration/03-UI-SPEC.md §Color — loading state background requirement (#0f172a = slate-900)
  </read_first>
  <action>
    Change the &lt;body&gt; tag in index.html from:
      &lt;body&gt;
    to:
      &lt;body style="background-color: #0f172a;"&gt;

    This is a one-line change. The inline style prevents the white flash on Android WebView during the pre-render window (UMP consent + AdMob.initialize()) per the UI-SPEC.md color contract.

    The inline style is intentionally overridden at runtime by style.css which applies bg-slate-950 to body once Tailwind loads — this is expected and documented in UI-SPEC.md (slate-900 vs slate-950 delta is not perceptible during the sub-1-second loading window).

    No other changes to index.html.
  </action>
  <verify>
    <automated>grep -c "background-color: #0f172a" "C:/code/cursor/SimonGame/index.html"</automated>
  </verify>
  <acceptance_criteria>
    - index.html &lt;body&gt; tag contains style="background-color: #0f172a;"
    - All other index.html content is unchanged (viewport meta tag, script tag, div#app all present)
    - npm run build exits 0 after this change (HTML is still valid)
  </acceptance_criteria>
  <done>index.html body has #0f172a background — no white flash during AdMob init window</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local install | Third-party packages downloaded and installed into node_modules |
| strings.xml → AndroidManifest.xml | App ID reference indirection — a wrong value here causes SDK crash at launch |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | npm install (@capacitor-community/admob, @capacitor/app) | mitigate | Package Legitimacy Audit in RESEARCH.md confirms both [OK]; no postinstall scripts; executor verifies npm list output after install |
| T-03-02 | Spoofing | strings.xml admob_app_id placeholder | accept | App IDs are not secrets (visible in any APK); placeholder value does not cause a security issue — only a runtime "no ad" symptom if wrong |
| T-03-03 | Denial of Service | Malformed AndroidManifest.xml XML | mitigate | Read full file before editing; preserve all existing attributes; validate XML structure by confirming npx cap sync android exits 0 |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | slopcheck confirmed [OK] for both packages per RESEARCH.md Package Legitimacy Audit; no blocking human checkpoint required |
</threat_model>

<verification>
After all three tasks complete:

1. npm list @capacitor-community/admob shows 8.0.0
2. npm list @capacitor/app shows 8.x.x
3. npm run build exits 0
4. npx cap sync android exits 0
5. AndroidManifest.xml contains com.google.android.gms.ads.APPLICATION_ID meta-data
6. strings.xml contains admob_app_id string resource
7. index.html body has background-color: #0f172a
</verification>

<success_criteria>
- ADM-01: @capacitor-community/admob@8.0.0 installed — SATISFIED when npm list shows 8.0.0
- ADM-02: AdMob App ID meta-data in AndroidManifest.xml pointing to @string/admob_app_id — SATISFIED when grep confirms the meta-data element
- Pre-render background: index.html body background matches slate-900 (#0f172a) — SATISFIED when grep confirms
- Build pipeline still green: npm run build and npx cap sync android both exit 0
</success_criteria>

<output>
Create `.planning/phases/03-admob-integration/03-01-SUMMARY.md` when done
</output>
