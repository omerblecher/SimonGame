---
phase: 03-admob-integration
plan: 03
type: execute
wave: 3
depends_on:
  - 03-02
files_modified:
  - src/App.tsx
autonomous: false
requirements:
  - ADM-05
  - ADM-06
  - ADM-07
  - ADM-08

must_haves:
  truths:
    - "App.tsx calls useBannerHeight() and uses the returned value for the spacer div height"
    - "A spacer <div style={{ height: bannerHeight + 'px' }} aria-hidden='true' /> is the last child of the outermost game container div"
    - "showBanner() is called in a useEffect on mount with BannerAdSize.BANNER, BannerAdPosition.BOTTOM_CENTER, test adId, isTesting: true"
    - "appStateChange listener hides banner on background (hideBanner), resumes on foreground (resumeBanner)"
    - "removeBanner() is called in useEffect cleanup on unmount — not on background"
    - "All AdMob/CapacitorApp calls in App.tsx are behind Capacitor.isNativePlatform() guards"
    - "A test banner ad (green 'Test Ad' bar) is visible at the bottom of the screen on the physical Android device"
    - "Game pads are not obscured by the banner (50px spacer reserves gap)"
    - "Banner disappears when app is backgrounded and reappears on return to foreground"
    - "Audio (tones and buzzer) is audible on the physical Android device during gameplay"
  artifacts:
    - path: "src/App.tsx"
      provides: "banner show/hide/resume logic + spacer div"
      contains: "useBannerHeight"
    - path: "src/App.tsx"
      provides: "appStateChange listener for ADM-08"
      contains: "CapacitorApp"
  key_links:
    - from: "useBannerHeight() return value"
      to: "spacer <div> inline style height"
      via: "bannerHeight variable in App component"
      pattern: "bannerHeight.*px"
    - from: "appStateChange listener"
      to: "AdMob.hideBanner() / AdMob.resumeBanner()"
      via: "isActive flag in AppStateChange payload"
      pattern: "isActive.*hideBanner|resumeBanner.*isActive"
---

<objective>
Wire the AdMob banner into App.tsx: add the useBannerHeight() hook call, the showBanner/appStateChange useEffect, and the spacer div. Then build, sync, and run on the physical Android device to verify the test banner, layout, background/foreground lifecycle, and audio.

Purpose: This plan completes the full AdMob integration by wiring the hook (Plan 02) into the component tree and verifying the observable behaviors — test banner visible, pads not obscured, banner hides/resumes on app state changes, and audio confirmed on real hardware.

Output: Updated App.tsx with banner logic and spacer; verified working Android build on physical device.
</objective>

<execution_context>
@C:/Users/omerb/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/omerb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/code/cursor/SimonGame/.planning/PROJECT.md
@C:/code/cursor/SimonGame/.planning/ROADMAP.md
@C:/code/cursor/SimonGame/.planning/STATE.md
@C:/code/cursor/SimonGame/.planning/phases/03-admob-integration/03-CONTEXT.md
@C:/code/cursor/SimonGame/.planning/phases/03-admob-integration/03-RESEARCH.md
@C:/code/cursor/SimonGame/.planning/phases/03-admob-integration/03-UI-SPEC.md
@C:/code/cursor/SimonGame/.planning/phases/03-admob-integration/03-02-SUMMARY.md

<interfaces>
<!-- src/App.tsx structure (relevant sections only) -->

Top of file imports (line 1):
  import React, { useCallback, useMemo, useRef, useState } from 'react';
  -- Must ADD: useEffect to the destructured import list
  -- Must ADD imports: Capacitor, AdMob + types, CapacitorApp, useBannerHeight

App component JSX return (line 286-464):
  Outer div (line 287): &lt;div className="min-h-screen bg-gradient-to-br from-slate-950 ...  flex items-center justify-center px-4 py-8"&gt;
    Inner div (line 288): &lt;div className="w-full max-w-4xl"&gt;
      Glass panel div (line 289): &lt;div className="glass-panel rounded-3xl px-6 py-6 ..."&gt;
        [header, game board, sidebar, how-to-play — lines 290-458]
      &lt;/div&gt; (line 459 — closes glass-panel)
    &lt;/div&gt; (line 460 — closes max-w-4xl)
  &lt;/div&gt; (line 461 — closes outermost min-h-screen div)
-- The spacer div is inserted INSIDE the outermost min-h-screen div, after the max-w-4xl div, before the closing &lt;/div&gt; on line 461

Current closing tag structure (lines 459-464):
          &lt;/div&gt;   ← closes glass-panel
        &lt;/div&gt;     ← closes max-w-4xl
      &lt;/div&gt;       ← closes outermost div (min-h-screen) ← spacer goes here, BEFORE this closing tag
    );
  };

-- IMPORTANT: The spacer div must be the last child of the outermost div (min-h-screen), not nested inside max-w-4xl or glass-panel. Per UI-SPEC.md §Banner Spacer Contract §Positioning.

<!-- useBannerHeight hook (created in Plan 02) -->
import { useBannerHeight } from './hooks/useBannerHeight';
// returns: number (0 in browser; 50 pre-load on native; actual banner height post-bannerAdSizeChanged)

<!-- API types from @capacitor-community/admob@8.0.0 -->
import {
  AdMob,
  BannerAdOptions,
  BannerAdSize,
  BannerAdPosition,
} from '@capacitor-community/admob';

const TEST_BANNER_ID = 'ca-app-pub-3940256099942544/6300978111'; // Google test banner unit ID

const bannerOptions: BannerAdOptions = {
  adId: TEST_BANNER_ID,
  adSize: BannerAdSize.BANNER,           // 320×50dp
  position: BannerAdPosition.BOTTOM_CENTER,
  margin: 0,
  isTesting: true,                       // Phase 3 only — per key decision #9
};

<!-- CapacitorApp for appStateChange (ADM-08) -->
import { App as CapacitorApp } from '@capacitor/app';
// CapacitorApp.addListener('appStateChange', ({ isActive }) => { ... })
// Returns: Promise&lt;{ remove: () => Promise&lt;void&gt; }&gt;
// isActive: true = foreground, false = background

<!-- Banner lifecycle rules (from RESEARCH.md Pitfall 6 and Anti-Patterns) -->
// On mount:       showBanner(options)
// On background:  hideBanner()    — keeps banner object alive for resume
// On foreground:  resumeBanner()  — reuses existing banner, no new ad request
// On unmount:     removeBanner()  — destroys banner object (useEffect cleanup)
// NEVER call removeBanner() on background — breaks resumeBanner()
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add banner hook, showBanner effect, appStateChange listener, and spacer div to App.tsx</name>
  <files>src/App.tsx</files>
  <read_first>
    - C:/code/cursor/SimonGame/src/App.tsx — read the full file (all 466 lines); focus on: line 1 imports, the return() JSX block starting line 286, and closing tags on lines 459-464
    - C:/code/cursor/SimonGame/.planning/phases/03-admob-integration/03-RESEARCH.md Pattern 3 — showBanner + AppStateChange code pattern (exact API signatures)
    - C:/code/cursor/SimonGame/.planning/phases/03-admob-integration/03-UI-SPEC.md §Banner Spacer Contract — spacer element spec (aria-hidden, inline style, placement)
    - C:/code/cursor/SimonGame/.planning/phases/03-admob-integration/03-RESEARCH.md §Anti-Patterns — hideBanner vs removeBanner rules
  </read_first>
  <action>
    Make three targeted edits to App.tsx. Do NOT refactor the existing 463-line monolith — add only what is needed.

    EDIT 1 — Update imports (line 1):
    Add useEffect to the existing React destructured import. Current line:
      import React, { useCallback, useMemo, useRef, useState } from 'react';
    Becomes:
      import React, { useCallback, useEffect, useMemo, useRef, useState } from 'react';

    Add three new import lines after the existing React import line:
      import { Capacitor } from '@capacitor/core';
      import { AdMob, BannerAdOptions, BannerAdSize, BannerAdPosition } from '@capacitor-community/admob';
      import { App as CapacitorApp } from '@capacitor/app';
      import { useBannerHeight } from './hooks/useBannerHeight';

    The CapacitorApp alias is MANDATORY — plain { App } from '@capacitor/app' would collide with the { App } component exported from this same file (Pitfall 5 from RESEARCH.md).

    EDIT 2 — Add hook call and banner useEffect inside the App component function body, before the return() statement:
    Add after the existing useState/useRef/useCallback/useMemo declarations and before the return():

    1. Hook call:
       const bannerHeight = useBannerHeight();

    2. Banner show/hide/lifecycle useEffect (no dependency array means deps = [] — runs once on mount):
       useEffect(() => {
         if (!Capacitor.isNativePlatform()) return;

         const TEST_BANNER_ID = 'ca-app-pub-3940256099942544/6300978111';
         const bannerOptions: BannerAdOptions = {
           adId: TEST_BANNER_ID,
           adSize: BannerAdSize.BANNER,
           position: BannerAdPosition.BOTTOM_CENTER,
           margin: 0,
           isTesting: true,
         };

         AdMob.showBanner(bannerOptions).catch(console.error);

         let appStateHandle: { remove: () => Promise&lt;void&gt; } | null = null;
         CapacitorApp.addListener('appStateChange', ({ isActive }) => {
           if (isActive) {
             AdMob.resumeBanner().catch(console.error);
           } else {
             AdMob.hideBanner().catch(console.error);
           }
         }).then((h) => {
           appStateHandle = h;
         });

         return () => {
           AdMob.removeBanner().catch(console.error);
           appStateHandle?.remove();
         };
       }, []);

    The dependency array must be [] (empty). The isNativePlatform() guard means this effect is a no-op in browser.

    EDIT 3 — Add spacer div to JSX return:
    The spacer div is inserted as the last child of the outermost div (min-h-screen div, currently closing on line ~461). Insert BEFORE the closing &lt;/div&gt; of the outermost container, AFTER the &lt;/div&gt; that closes the max-w-4xl container:

      &lt;div style={{ height: `${bannerHeight}px` }} aria-hidden="true" /&gt;

    The spacer participates in normal document flow (not positioned, not fixed). In browser (bannerHeight = 0), it has zero height and is invisible. On native, it pre-reserves 50px, then updates to the actual banner height from bannerAdSizeChanged.

    TypeScript strict mode is on (noUnusedLocals: true). Ensure bannerHeight is used in the JSX (the spacer div). Ensure all imported identifiers are used. After all edits, run npm run build to confirm zero TypeScript errors.

    Per UI-SPEC.md §Banner Spacer Contract §Positioning: the spacer must NOT be inside glass-panel div or max-w-4xl div — it must be a direct child of the outermost min-h-screen div.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - src/App.tsx imports useEffect (added to React destructured import)
    - src/App.tsx imports { App as CapacitorApp } from '@capacitor/app' (aliased — not plain App)
    - src/App.tsx imports { useBannerHeight } from './hooks/useBannerHeight'
    - src/App.tsx calls const bannerHeight = useBannerHeight() inside the App function body
    - src/App.tsx contains AdMob.showBanner() inside a useEffect with [] deps
    - src/App.tsx useEffect cleanup calls AdMob.removeBanner() (not hideBanner)
    - src/App.tsx contains CapacitorApp.addListener('appStateChange', ...) inside the same useEffect
    - src/App.tsx contains AdMob.resumeBanner() for the isActive=true branch
    - src/App.tsx contains AdMob.hideBanner() for the isActive=false branch
    - src/App.tsx contains &lt;div style={{ height: `${bannerHeight}px` }} aria-hidden="true" /&gt; as last child of outermost div
    - The spacer div is NOT inside the glass-panel or max-w-4xl divs
    - npm run build exits 0 with zero TypeScript errors
    - npx cap sync android exits 0 after build
  </acceptance_criteria>
  <done>App.tsx wired with banner lifecycle and spacer; build passes; cap sync completes</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Physical device verification — banner, layout, lifecycle, and audio</name>
  <what-built>
    Full AdMob integration: @capacitor-community/admob@8.0.0 installed, Android manifest configured with App ID meta-data, GDPR/UMP consent sequence in main.tsx, useBannerHeight hook listening to bannerAdSizeChanged, showBanner/hideBanner/resumeBanner lifecycle in App.tsx, and spacer div reserving banner height. Plans 01-03 are now complete.
  </what-built>
  <how-to-verify>
    Prerequisites before running:
    - Confirm you have replaced YOUR_ADMOB_APP_ID in android/app/src/main/res/values/strings.xml with your real AdMob App ID (format: ca-app-pub-XXXXXXXXXXXXXXXX~XXXXXXXXXX with tilde separator).
    - Connect your physical Android device via USB with USB debugging enabled.
    - Set JAVA_HOME: run in PowerShell: $env:JAVA_HOME = "C:/Program Files/Eclipse Adoptium/jdk-21.0.7.6-hotspot"

    Build and deploy:
    1. npm run build  (must exit 0)
    2. npx cap sync android  (must exit 0)
    3. Open Android Studio → open android/ project → Run on connected device
       OR: cd android && .\gradlew installDebug  then adb shell am start -n com.otis.brooke.simon.game/.MainActivity

    Verification checklist (check each):

    BANNER (ADM-05):
    - [ ] A green "Test Ad" banner is visible at the bottom of the screen
    - [ ] The banner appears within ~2 seconds of app launch

    LAYOUT (ADM-06):
    - [ ] Game pads (green/red/yellow/blue) are fully visible and not obscured by the banner
    - [ ] There is a visible gap between the bottom Simon pad and the top edge of the banner

    GAMEPLAY (regression check — must still work after AdMob changes):
    - [ ] Tap "Start Game" — sequence plays and pads light up correctly
    - [ ] Tap pads in correct sequence — score increments
    - [ ] Tap wrong pad — game over and reset works correctly

    AUDIO (D-11 — deferred from Phase 2, required in Phase 3):
    - [ ] Tones play when pads light up during sequence playback
    - [ ] Tones play when you tap pads during input phase
    - [ ] Buzzer sound plays on incorrect tap (game over)
    - [ ] (Optional) Celebration melody plays after 5 consecutive correct rounds

    BACKGROUND/FOREGROUND LIFECYCLE (ADM-08):
    - [ ] Press the Android home button while the app is open — the banner disappears
    - [ ] Return to the app (tap it in recents or the launcher) — the banner reappears
    - [ ] Game state is preserved after backgrounding and returning (no game reset)

    CONSENT DIALOG (ADM-03 — EEA users only):
    - [ ] If you are in the EEA/UK: a native Google consent dialog appeared on first launch before the game UI
    - [ ] If you are NOT in the EEA/UK (e.g., US): no dialog appeared — app launched directly into the game (expected behavior per D-10)
  </how-to-verify>
  <resume-signal>
    Type "approved" if ALL checklist items pass (or "approved - no consent dialog" if outside EEA and consent check is N/A).
    Type "issues: [describe]" if anything fails — include which checklist item failed and what you observed.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Native AdMob overlay → WebView layout | Banner renders in native layer above WebView; spacer div ensures touch targets are not in the obscured zone |
| appStateChange events → banner lifecycle | Native app lifecycle events drive banner visibility — wrong handler (removeBanner vs hideBanner) would break foreground resume |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | Denial of Service | showBanner() called before AdMob.initialize() | mitigate | main.tsx (Plan 02) awaits initializeAdMob() before React renders; App.tsx showBanner() useEffect runs after mount, which only happens after render completes — correct ordering guaranteed |
| T-03-08 | Tampering | removeBanner() called on background instead of hideBanner() | mitigate | Acceptance criteria explicitly checks that removeBanner() is in cleanup (unmount) and hideBanner() is in the isActive=false branch; reviewer catches this at build verification |
| T-03-09 | Information Disclosure | Test banner adId hardcoded in App.tsx source | accept | Test ID (ca-app-pub-3940256099942544/6300978111) is Google's public test ID; it is designed to be in source code. Production ID replacement happens in Phase 4 (STORE-05). |
| T-03-10 | Elevation of Privilege | CapacitorApp listener not removed on unmount | mitigate | appStateHandle?.remove() called in useEffect cleanup alongside removeBanner(); both listeners deregistered on component unmount |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed in this plan; packages installed and verified in Plan 01 |
</threat_model>

<verification>
After both tasks complete:

1. npm run build exits 0 — zero TypeScript errors in App.tsx
2. grep confirms useBannerHeight in App.tsx
3. grep confirms CapacitorApp in App.tsx (the aliased import)
4. grep confirms bannerHeight in App.tsx spacer div style
5. grep confirms AdMob.removeBanner in App.tsx (cleanup)
6. grep confirms AdMob.hideBanner in App.tsx (background)
7. grep confirms AdMob.resumeBanner in App.tsx (foreground)
8. Human checkpoint passed — user confirms banner visible, pads not obscured, audio works, background/foreground lifecycle works
</verification>

<success_criteria>
- ADM-05: Test banner visible at bottom of screen on physical device — SATISFIED by human checkpoint
- ADM-06: 50dp+ gap between banner and pads — SATISFIED by 50px spacer pre-reserve + bannerAdSizeChanged update
- ADM-07: bannerAdSizeChanged event used for dynamic spacer height — SATISFIED by useBannerHeight hook using BannerAdPluginEvents.SizeChanged
- ADM-08: Banner hidden on background, shown on foreground — SATISFIED by appStateChange listener in App.tsx useEffect
- D-11 (Phase 2 deferred): Audio audible on physical Android device — SATISFIED by human checkpoint audio verification
- All 8 ADM requirements (ADM-01 through ADM-08) are now satisfied across Plans 01-03
</success_criteria>

<output>
Create `.planning/phases/03-admob-integration/03-03-SUMMARY.md` when done
</output>
