---
phase: 02-capacitor-android-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - capacitor.config.ts
  - package.json
  - package-lock.json
autonomous: true
requirements:
  - CAP-01
  - CAP-02
  - CAP-03
  - CAP-04

must_haves:
  truths:
    - "Capacitor 8 is confirmed as required via npm show @capacitor-community/admob"
    - "@capacitor/core, @capacitor/android (^8) and @capacitor/cli (^8 dev) are installed"
    - "capacitor.config.ts exists at project root with correct appId, appName, webDir, and SystemBars.hidden"
    - "android/ directory exists with app/build.gradle containing applicationId com.otis.brooke.simon.game"
    - "android/app/src/main/res/values/strings.xml app_name equals Simon Memory Game"
  artifacts:
    - path: "capacitor.config.ts"
      provides: "Capacitor app identity and plugin config"
      contains: "com.otis.brooke.simon.game"
    - path: "android/app/build.gradle"
      provides: "Android Gradle build config"
      contains: "com.otis.brooke.simon.game"
    - path: "android/app/src/main/res/values/strings.xml"
      provides: "Android string resources"
      contains: "Simon Memory Game"
  key_links:
    - from: "capacitor.config.ts"
      to: "android/app/build.gradle"
      via: "npx cap add android reads appId from config and writes applicationId to build.gradle"
      pattern: "applicationId.*com\\.otis\\.brooke\\.simon\\.game"
---

<objective>
Verify the environment, confirm the required Capacitor version via AdMob compatibility check, install Capacitor 8 packages, create capacitor.config.ts, and scaffold the Android project.

Purpose: Everything in Phase 2 depends on this wave — correct Capacitor version (CAP-01) gates Phase 3 AdMob integration; the config (CAP-03) and scaffold (CAP-04) gate the build pipeline in Plan 02.

Output: capacitor.config.ts at project root; android/ directory scaffolded with correct applicationId and app_name.
</objective>

<execution_context>
@C:/Users/omerb/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/omerb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/code/cursor/SimonGame/.planning/PROJECT.md
@C:/code/cursor/SimonGame/.planning/ROADMAP.md
@C:/code/cursor/SimonGame/.planning/STATE.md
@C:/code/cursor/SimonGame/.planning/phases/02-capacitor-android-baseline/02-CONTEXT.md
@C:/code/cursor/SimonGame/.planning/phases/02-capacitor-android-baseline/02-RESEARCH.md

<interfaces>
From package.json (current state — no Capacitor packages present):
  dependencies: react, react-dom, @types/react, @types/react-dom
  devDependencies: vite, typescript, tailwindcss, postcss, autoprefixer
  scripts.build: "tsc && vite build"  (outputs to dist/)

capacitor.config.ts — does NOT exist yet; Task 3 creates it.
android/ directory — does NOT exist yet; Task 3 creates it via npx cap add android.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify prerequisites and confirm Capacitor 8 requirement (CAP-01)</name>
  <files>No files modified — diagnostic only</files>
  <read_first>
    - C:/code/cursor/SimonGame/package.json (confirm no Capacitor packages already installed)
    - C:/code/cursor/SimonGame/.planning/phases/02-capacitor-android-baseline/02-RESEARCH.md sections 1 and 10 (prerequisite table and verification commands)
  </read_first>
  <action>
Run each prerequisite check in PowerShell and record the result. If any check fails, stop and report before proceeding to Task 2.

Check 1 — Java 17+:
  java -version
  Required: the output must contain "17" or higher (e.g., "version 17.0.x" or "version 21.0.x"). If the output shows version 11, 8, or anything below 17, STOP — Java 17+ must be installed before continuing.

Check 2 — ANDROID_HOME:
  $env:ANDROID_HOME
  Required: a non-empty filesystem path, typically C:\Users\omerb\AppData\Local\Android\Sdk. If empty or unset, STOP — ANDROID_HOME must be set in system environment variables before continuing.

Check 3 — AVD present:
  emulator -list-avds
  Required: at least one AVD name is printed. If no output is produced, STOP — create an AVD in Android Studio's AVD Manager (API 24+ system image) before continuing.

Check 4 — AdMob version confirmation (CAP-01, per D-03):
  npm show @capacitor-community/admob
  Locate the "latest:" line. It must show 8.x.x. This confirms @capacitor/core ^8.0.0 is the required Capacitor major. Per RESEARCH.md section 1: the admob plugin version scheme tracks Capacitor major (admob@8 requires Capacitor 8).

  Also run:
  npm show @capacitor-community/admob dependencies
  The output must include: { '@capacitor/core': '^8.0.0' }

Record all four results. Proceed to Task 2 only if all four checks pass.
  </action>
  <verify>
    <automated>java -version 2>&1 | Select-String "version"; Write-Host "ANDROID_HOME: $env:ANDROID_HOME"; emulator -list-avds; npm show @capacitor-community/admob | Select-String "latest"</automated>
  </verify>
  <acceptance_criteria>
    - java -version output contains "17" or higher — FAIL if it shows 11 or lower
    - $env:ANDROID_HOME prints a non-empty path — FAIL if empty
    - emulator -list-avds prints at least one AVD name — FAIL if no output
    - npm show @capacitor-community/admob shows "latest: 8." — FAIL if 6.x or 7.x
    - npm show @capacitor-community/admob dependencies includes "@capacitor/core" with value "^8.0.0"
  </acceptance_criteria>
  <done>All four checks pass; stdout confirms admob latest is 8.x.x requiring @capacitor/core ^8.0.0</done>
</task>

<task type="auto">
  <name>Task 2: Install Capacitor 8 packages (CAP-02)</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    - C:/code/cursor/SimonGame/package.json (record current dependencies before install)
    - C:/code/cursor/SimonGame/.planning/phases/02-capacitor-android-baseline/02-RESEARCH.md section 2 (exact install commands and expected package.json result)
  </read_first>
  <action>
Install Capacitor 8 packages. Do NOT run npx cap init — the config file is created manually in Task 3 to lock in D-01 and D-02 decisions without interactive prompts (per RESEARCH.md section 2).

Run in PowerShell:

Step 1 — Install runtime packages:
  npm install @capacitor/core@^8 @capacitor/android@^8

Step 2 — Install CLI as dev dependency:
  npm install -D @capacitor/cli@^8

After both commands complete, verify package.json was updated:
  node -e "const p=require('./package.json'); ['@capacitor/core','@capacitor/android'].forEach(k=>{const v=p.dependencies[k]; console.log(k, v, v?.startsWith('^8')?'PASS':'FAIL')}); const v2=p.devDependencies['@capacitor/cli']; console.log('@capacitor/cli (dev)', v2, v2?.startsWith('^8')?'PASS':'FAIL')"

All three lines must print PASS.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); ['@capacitor/core','@capacitor/android'].forEach(k=>{const v=p.dependencies[k]; console.log(k, v, v?.startsWith('^8')?'PASS':'FAIL')}); const v2=p.devDependencies['@capacitor/cli']; console.log('@capacitor/cli (dev)', v2, v2?.startsWith('^8')?'PASS':'FAIL')"</automated>
  </verify>
  <acceptance_criteria>
    - package.json dependencies contains "@capacitor/core" with value starting "^8" — FAIL if absent or wrong major
    - package.json dependencies contains "@capacitor/android" with value starting "^8" — FAIL if absent or wrong major
    - package.json devDependencies contains "@capacitor/cli" with value starting "^8" — FAIL if absent or wrong major
    - node_modules/@capacitor/core/package.json exists (i.e., packages were actually installed, not just listed)
  </acceptance_criteria>
  <done>All three Capacitor 8 packages installed; verification script prints PASS for all three entries</done>
</task>

<task type="auto">
  <name>Task 3: Create capacitor.config.ts, scaffold Android project, and verify identity fields (CAP-03, CAP-04)</name>
  <files>
    capacitor.config.ts,
    android/app/build.gradle,
    android/app/src/main/res/values/strings.xml
  </files>
  <read_first>
    - C:/code/cursor/SimonGame/.planning/phases/02-capacitor-android-baseline/02-RESEARCH.md sections 3, 4, and 9 (config template, scaffold command, strings.xml and build.gradle verification)
    - C:/code/cursor/SimonGame/.planning/phases/02-capacitor-android-baseline/02-CONTEXT.md decisions D-01, D-02, D-03, D-07 (appId, appName, Capacitor version, SystemBars)
  </read_first>
  <action>
Step 1 — Create capacitor.config.ts at the project root (C:/code/cursor/SimonGame/capacitor.config.ts).

Write this exact content (TypeScript, no JSON — consistent with project's TS-first convention):

  import type { CapacitorConfig } from '@capacitor/cli';

  const config: CapacitorConfig = {
    appId: 'com.otis.brooke.simon.game',
    appName: 'Simon Memory Game',
    webDir: 'dist',
    plugins: {
      SystemBars: {
        hidden: true,
      },
    },
  };

  export default config;

Decisions honored:
  - D-01: appId = 'com.otis.brooke.simon.game' (permanent; 4-segment reverse-domain is valid for Android/Gradle)
  - D-02: appName = 'Simon Memory Game'
  - D-07: plugins.SystemBars.hidden = true (Capacitor 8 built-in; hides status bar and nav bar at launch without an external plugin)
  - webDir = 'dist' matches npm run build output directory

Step 2 — Scaffold the Android project:
  npx cap add android

This creates the android/ directory. The command reads appId from capacitor.config.ts and writes applicationId to android/app/build.gradle automatically. It also creates android/app/src/main/res/values/strings.xml.

Wait for the command to complete fully (may take 30-60 seconds on first run as Gradle wrapper downloads).

Step 3 — Verify applicationId in build.gradle (CAP-04, D-01):
  Select-String "com.otis.brooke.simon.game" android/app/build.gradle

Expected: at least one match on a line containing "applicationId". If the line is missing or shows a different value, manually edit android/app/build.gradle and replace the applicationId value with "com.otis.brooke.simon.game".

Step 4 — Verify app_name in strings.xml (D-02):
  Select-String "app_name" android/app/src/main/res/values/strings.xml

Expected: a line containing both app_name and Simon Memory Game. If the value is wrong (e.g., it shows the folder name "SimonGame" or "simongame"), edit the file and set:
  <string name="app_name">Simon Memory Game</string>
  </action>
  <verify>
    <automated>
      Test-Path C:/code/cursor/SimonGame/capacitor.config.ts;
      Select-String "com.otis.brooke.simon.game" C:/code/cursor/SimonGame/capacitor.config.ts;
      Select-String "Simon Memory Game" C:/code/cursor/SimonGame/capacitor.config.ts;
      Select-String "hidden: true" C:/code/cursor/SimonGame/capacitor.config.ts;
      Test-Path C:/code/cursor/SimonGame/android/app/build.gradle;
      Select-String "com.otis.brooke.simon.game" C:/code/cursor/SimonGame/android/app/build.gradle;
      Select-String "Simon Memory Game" C:/code/cursor/SimonGame/android/app/src/main/res/values/strings.xml
    </automated>
  </verify>
  <acceptance_criteria>
    - capacitor.config.ts exists at C:/code/cursor/SimonGame/capacitor.config.ts
    - capacitor.config.ts contains the exact string "com.otis.brooke.simon.game" on the appId line (D-01)
    - capacitor.config.ts contains the exact string "Simon Memory Game" on the appName line (D-02)
    - capacitor.config.ts contains "SystemBars" and "hidden: true" in the plugins block (D-07)
    - capacitor.config.ts contains "webDir: 'dist'" (not 'build', 'public', or any other value)
    - android/app/build.gradle exists
    - android/app/build.gradle contains "com.otis.brooke.simon.game" on a line with "applicationId" (D-01)
    - android/app/src/main/res/values/strings.xml contains a string element with name="app_name" and value "Simon Memory Game" (D-02)
  </acceptance_criteria>
  <done>capacitor.config.ts created with correct appId/appName/webDir/SystemBars; android/ scaffolded; applicationId and app_name verified correct</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry -> local node_modules | Third-party packages installed from public registry |
| capacitor.config.ts -> android/build.gradle | App identity propagated from config to native project |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | npm install @capacitor/* | mitigate | Verify installed major version is 8 via node -e check after install; mismatched version would break Phase 3 AdMob |
| T-02-02 | Tampering | applicationId in build.gradle | mitigate | Explicitly verify Select-String output after cap add android; correct manually if wrong |
| T-02-SC | Tampering | npm package installs | mitigate | @capacitor/core, @capacitor/android, @capacitor/cli are established Ionic Foundation packages — verified via npm show before install |
</threat_model>

<verification>
All three tasks must pass before Plan 02 can execute:

1. CAP-01: npm show @capacitor-community/admob confirms latest is 8.x.x
2. CAP-02: package.json has @capacitor/core ^8, @capacitor/android ^8, @capacitor/cli ^8 (dev)
3. CAP-03: capacitor.config.ts contains com.otis.brooke.simon.game + Simon Memory Game + dist + SystemBars hidden
4. CAP-04: android/ exists; build.gradle has applicationId com.otis.brooke.simon.game; strings.xml has app_name Simon Memory Game
</verification>

<success_criteria>
- npm show @capacitor-community/admob shows latest: 8.x.x (CAP-01)
- node verification script prints PASS for all three @capacitor/* packages at ^8 (CAP-02)
- capacitor.config.ts exists with appId=com.otis.brooke.simon.game, appName=Simon Memory Game, webDir=dist, SystemBars.hidden=true (CAP-03, D-01, D-02, D-07)
- android/ directory scaffolded; build.gradle applicationId=com.otis.brooke.simon.game; strings.xml app_name=Simon Memory Game (CAP-04, D-01, D-02)
</success_criteria>

<output>
Create .planning/phases/02-capacitor-android-baseline/02-01-SUMMARY.md when done
</output>
